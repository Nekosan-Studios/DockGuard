# Frontend Unit Testing Integration

- [x] Install dependencies (`vitest`, `@testing-library/svelte`, `@testing-library/jest-dom`, `jsdom`)
- [x] Configure `vite.config.ts` for Vitest and `jsdom` environment
- [x] Create `setupTest.ts` to globally inject DOM matchers
- [x] Add `test:unit` script to `package.json`
- [x] Write a test for a utility function (e.g. formatting dates or severities)
- [x] Write a test for a Svelte UI component (e.g. rendering a vulnerability badge)
- [x] Run the tests and verify they pass
- [x] Generate a final walkthrough for the frontend testing implementation
